spring:
    cloud:
      gateway:
        routes:

          - id: fournisseur
            uri: lb://FOURNISSEUR
            predicates:
              - Path=/fournisseurs/**


    application:
      name: API-GETWAY
server:
  port: 8081


eureka:
  client:
      service-url:
        defaultZone: http://localhost:8761/eureka/

